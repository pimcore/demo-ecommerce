pimcore:
    # CLASS OVERRIDES
    models:
        class_overrides:
            Pimcore\Model\Object\Product: AppBundle\Model\DefaultProduct
            Pimcore\Model\Object\ProductCategory: AppBundle\Model\ShopCategory

imports:

    # e-commerce framework configuration
    - { resource: ecommerce/ecommerce-config.yml }

    # optionally include file for overwrite services
    - { resource: ecommerce/service-overrides.yml }